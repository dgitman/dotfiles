# bin

Personal command-line helpers that are intentionally kept on `PATH`.

## Commands

| Command | Purpose |
| --- | --- |
| `brewfile-update` | Regenerate `brew/Brewfile`, commit it, and push it. |
| `agent` / `cursor-agent` | Cursor agent symlinks. |
